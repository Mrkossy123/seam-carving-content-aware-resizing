# Seam Carving for Content-Aware Image Resizing

A Python implementation of the **Seam Carving** algorithm for content-aware image resizing, based on the SIGGRAPH 2007 paper by **Shai Avidan and Ariel Shamir**.

Unlike standard image resizing, which scales the entire image uniformly, seam carving removes paths of low-importance pixels called **seams**. This allows an image to be resized while preserving visually important content as much as possible.

---

## Overview

This project implements a classical Computer Vision algorithm for intelligent image resizing.  
The method identifies low-energy paths in an image and removes them iteratively in order to reduce either the width or the height of the image.

The project includes:

- Energy function computation using image gradients
- Vertical seam detection
- Horizontal seam detection
- Width reduction using vertical seam removal
- Height reduction using horizontal seam removal
- Seam visualization on the original image
- Comparison between seam carving and standard resizing
- Demo scripts for experimentation with different images

---

## What Is Seam Carving?

A **seam** is a connected path of pixels that crosses the image from one side to the other.

There are two types of seams:

- **Vertical seam**: a connected path from top to bottom
- **Horizontal seam**: a connected path from left to right

Each seam contains one pixel per row or column, and the goal is to find the seam with the lowest total energy.

Pixels with low energy usually belong to less important regions of the image, such as background areas, smooth textures, or visually insignificant parts.

---

## Algorithm

The algorithm follows these main steps:

1. Convert the input image to grayscale.
2. Compute the energy of each pixel using the magnitude of the image gradients.
3. Build a cumulative minimum energy map using dynamic programming.
4. Backtrack through the cumulative map to find the optimal seam.
5. Remove the selected seam from the image.
6. Repeat the process until the desired number of pixels has been removed.

The energy function used is:

```text
energy = |dI/dx| + |dI/dy|


Features
Content-aware width reduction
Content-aware height reduction
Vertical seam computation
Horizontal seam computation
Cumulative energy map visualization
Seam overlay visualization
Standard resizing comparison
Fully commented Python implementation
Modular code structure for experimentation
Technologies Used
Python
NumPy
Pillow
Matplotlib
SciPy
Installation

Clone the repository:

git clone https://github.com/your-username/seam-carving-content-aware-resizing.git
cd seam-carving-content-aware-resizing

Install the required dependencies:

pip install numpy pillow matplotlib scipy
Usage
Run the default assignment-style demo
python seam_carving_assignment.py --austin austin.jpg --disney disney.jpg --num_pixels 100 --out results

This command:

Reduces the width or height of the provided images
Saves the output images
Displays energy maps
Displays selected seams
Produces comparison results
Run seam carving on your own image

Reduce image width:

python seam_carving_assignment.py --image input.jpg --reduce_width 100 --out results

Reduce image height:

python seam_carving_assignment.py --image input.jpg --reduce_height 100 --out results

Reduce both width and height:

python seam_carving_assignment.py --image input.jpg --reduce_width 80 --reduce_height 50 --out results
Core Functions

The project includes the following main functions:

compute_energy(image)

Computes the energy map of an image using image gradients.

find_vertical_seam(energy)

Finds the minimum-energy vertical seam.

find_horizontal_seam(energy)

Finds the minimum-energy horizontal seam.

reduce_width(image_in, num_pixels)

Reduces the width of an image by removing vertical seams.

reduce_height(image_in, num_pixels)

Reduces the height of an image by removing horizontal seams.

draw_seam(image, seam, orientation)

Draws the selected seam on top of the image for visualization.

Example Results

The following outputs can be generated by the system:

Original input image
Energy map
Cumulative minimum energy map
First selected vertical seam
First selected horizontal seam
Seam-carved resized image
Standard resized image for comparison

Example comparison:

Original Image  →  Seam Carving Result  →  Standard Resizing Result

Seam carving usually performs better when the image contains large low-detail background regions.
It may perform worse when the image is dense, contains many important objects, or when important structures span the entire image.

Seam Carving vs Standard Resizing

Standard resizing scales all image regions equally. This can distort important objects, faces, buildings, or natural structures.

Seam carving attempts to preserve important content by removing less noticeable pixels first.

However, seam carving is not always better. It may produce artifacts when:

The image is visually dense
There is no clear low-energy background
Important objects cover most of the image
Repeated seam removal distorts geometric structures
Straight lines or regular patterns are present
